# Pip fallback (CPU/GPU). For the CUDA-pinned build used in the paper, prefer
# environment.yml. Install the package itself with:  pip install -e .

# Core
torch>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.2
scikit-image>=0.21
matplotlib>=3.7
openpyxl>=3.1

# Classical baselines / metrics
PyWavelets>=1.4
dtaidistance>=2.3

# Config systems used by the baseline trainers
ml-collections>=0.1.1
absl-py>=1.4

# Training utilities
tensorboard>=2.12
tqdm>=4.64
PyYAML>=6.0

# Dev / testing
pytest>=7.0
pytest-cov>=4.0
